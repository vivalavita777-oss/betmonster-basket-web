export default function OfflinePage() {
  return (
    <section className="pageStack">
      <div className="stateBox danger">
        <strong>OFFLINE / STALE</strong>
        <p>The app is showing the cached shell. Live responses are not cached as final results.</p>
      </div>
    </section>
  );
}
